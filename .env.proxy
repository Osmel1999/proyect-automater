# Bright Data ISP Proxy Configuration
# Este archivo contiene las credenciales del proxy ISP para pruebas

# Proxy ISP (Residential Static IP)
ISP_PROXY_HOST=brd.superproxy.io
ISP_PROXY_PORT=33335
ISP_PROXY_USERNAME=brd-customer-hl_e851436d-zone-isp_proxy1
ISP_PROXY_PASSWORD=bcej6jmzlv66

# País por defecto: Colombia
ISP_PROXY_COUNTRY=co

# Límite de requests: 1000 req/min
ISP_PROXY_RATE_LIMIT=1000

# Test URL
ISP_PROXY_TEST_URL=https://geo.brdtest.com/welcome.txt?product=isp&method=native
